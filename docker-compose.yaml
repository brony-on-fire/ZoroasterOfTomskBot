# Use root/example as user/password credentials
version: '3.8'

networks:
  bot_network:

services:

  python:
    build: ./python
    container_name: zoroaster
    restart: always
    environment:
      - BOT_TOKEN
      - POSTGRES_DB
      - POSTGRES_USER
      - POSTGRES_PASSWORD
    networks:
      - bot_network
    volumes:
      - ./app:/usr/src/app

  postgres:
    build: ./postgres
    container_name: postgres_db
    restart: always
    environment:
      - POSTGRES_DB
      - POSTGRES_USER
      - POSTGRES_PASSWORD
      - PGDATA
    networks:
      - bot_network
    volumes:
      - .:/var/lib/postgresql/data

  mongo:
    image: mongo
    container_name: mongo_db
    restart: always
    environment:
      - MONGO_INITDB_ROOT_USERNAME
      - MONGO_INITDB_ROOT_PASSWORD
    networks:
      - bot_network
    volumes:
      - ./mongodata:/data/db

  adminer:
     image: adminer
     restart: always
     networks:
      - bot_network
     ports:
      - 8080:8080